# helmoro_base

In this package you will find everything that is needed to run HelMoRo. We differentiate between common packages, that are used both in simulation and the real robot, simulation packages and packages only running on the real robot.